using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SC_MovimientoAscensor : MonoBehaviour
{//esta script lo que ara es quitar el movimiento normal del jugador y lo cambiara por otro
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
    QuitarMovimientoNormal();
    }

  
  void QuitarMovimientoNormal()
  {

  }
}
