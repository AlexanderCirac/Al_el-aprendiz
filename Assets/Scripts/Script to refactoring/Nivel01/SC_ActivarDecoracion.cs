using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SC_ActivarDecoracion : MonoBehaviour
{
  public GameObject m_escenario;
    // Start is called before the first frame update
    void Start()
    {
    m_escenario.SetActive(true);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
